# Test dependencies.
#
# This is the full app dependency set MINUS the heavy ML model libraries
# (easyocr -> torch, deepface -> tensorflow). Those are lazy-imported in the
# service layer and mocked in the unit tests, so installing them in CI would
# add gigabytes and several minutes for no test coverage benefit.

fastapi>=0.111.0
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Vision (cv2 is imported lazily by the OCR/liveness/face services and is
# exercised for real by the liveness tests). The headless wheel needs no
# system GL libraries.
opencv-python-headless>=4.9.0
Pillow>=10.3.0
numpy>=1.26.0

# IP intelligence / storage / db / cache / http
geoip2>=4.8.0
boto3>=1.34.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
redis[hiredis]>=5.0.0
httpx>=0.27.0

# Observability (imported by app.main)
prometheus-fastapi-instrumentator>=7.0.0

# Test runner
pytest>=8.0.0
pytest-asyncio>=0.23.0
